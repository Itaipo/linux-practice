# Git Aliases Cheat Sheet

These aliases are configured globally using `git config --global`.

## Status
git gs

## Pull
git gp

## Push
git gpush

## Add + Commit
git gsave -m "commit message"

## Log (pretty)
git gl

## Diff
git gd
